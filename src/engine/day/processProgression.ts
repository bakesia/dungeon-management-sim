import type { GameState } from '../../types/game'

export function processProgression(state: GameState): GameState {
  // Tier 2–5 progression rules are introduced after the core management loop.
  return state
}
